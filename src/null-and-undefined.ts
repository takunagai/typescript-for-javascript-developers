export {};

/**
 * null 型
 */
let absence: null = null; // null だけを許容したい場合
// absence = 'hello'; // null 型でないのでエラー

/**
 * undefined 型
 */
let data: undefined = undefined; // undefined だけを許容したい場合
// data = 123; // undefined でないのでエラー